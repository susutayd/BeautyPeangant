<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.spaapp">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/applogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpaApp"
        tools:replace="android:icon"
        >
        <activity android:name=".users.BookServices" />
        <activity android:name=".users.ViewServices" />
        <activity android:name=".admin.ConBookingsAdmin" />
        <activity android:name=".admin.BookingsAdmin" />
        <activity android:name=".dashboards.AdminDash" />
        <activity android:name=".dashboards.AdminDashboard" />
        <activity android:name=".dashboards.UserDashboard" />
        <activity android:name=".SelectionScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>